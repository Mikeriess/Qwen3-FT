apiVersion: v1
kind: Pod
metadata:
  name: axolotl-gpu
spec:
  containers:
    - name: axolotl
      image: nvcr.io/nvidia/pytorch:24.04-py3
      command: ["/bin/bash", "-c", "--"]
      args: ["sleep infinity"]
      resources:
        limits:
          nvidia.com/gpu: 4
  restartPolicy: Never